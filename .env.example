# Obtain a key from https://api.open.fec.gov/developers/
FEC_API_KEY=replace_with_your_openfec_api_key

# Optional app-level protection for hosted deployments.
# When APP_PASSWORD is set, browsers must authenticate before using the app.
APP_USERNAME=researcher
APP_PASSWORD=replace_with_a_long_private_password
